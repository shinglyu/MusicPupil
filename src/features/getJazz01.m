%from 2005_Modeling expressive music performance in jazz_Ramirez
%RULE 1
function fitJazz01 = getJazz01(scores, metadata)
   pos = getMetircalPos(socres, metadata);
   for scoreNo = 1: length(scores)
      for noteNo = 1:size(scores{scoreNo}, 1)
         prevNoteDura = 
         if scores{scoreNo}(noteNo,DURATION_BEATS) < 

end

